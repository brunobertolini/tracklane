## What changed

<!-- One or two sentences. Link the issue if there is one. -->

## Checklist

- [ ] Tests cover the change (`pnpm test`)
- [ ] `pnpm check` passes locally
- [ ] A changeset is included (`pnpm changeset`) — skip only for docs/CI-only changes
